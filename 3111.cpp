#include <iostream>
#include <string>
#include <stack>

using namespace std;

string T, A;
stack<char, int> L, R;

int main()
{
    cin.tie(0);
    ios::sync_with_stdio(0);

    cin >> A >> T;

    int l = 0, r = A.size() - 1;
    bool forward = true;


    return 0;
}